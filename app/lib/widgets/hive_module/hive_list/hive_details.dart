import 'package:flutter/material.dart';

class HiveDetails extends StatelessWidget {
  const HiveDetails({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}