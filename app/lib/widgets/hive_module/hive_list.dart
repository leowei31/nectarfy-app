import 'package:flutter/material.dart';

class HiveList extends StatelessWidget {
  
  
  const HiveList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('List of hives');
  }
}